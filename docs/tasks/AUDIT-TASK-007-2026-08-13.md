# TASK-007 Code Audit — 2026-08-13

## Metadata

- **Reviewer:** orchestrator session (this conversation)
- **Task:** TASK-007 — CV import, immutable source persistence, and local text extraction
- **Commit reviewed:** `80d8e9a` (squash merge of `feat/task-007-cv-import`)
- **Branch:** `main`
- **Date:** 2026-08-13
- **Method:** Manual file-by-file review of 39 files (+3,403/−31). No code was changed during
  the review.
- **Scope:** `src/profile/` (14 new files), `src/cli.ts` (changes), `src/persistence/resolve-migrations.ts` (new),
  `src/persistence/repositories/profile-sources.ts` (changes), all new tests + fixtures, documentation.

## Summary

| Severity | Count |
|---|---|
| Critical | 3 |
| Important | 9 |
| Minor | 10 |
| Verification (pre-existing) | 5 |
| Positive findings | 7 |

## How to use this document

This is a **checklist**, not a retrospective. Each item is self-describing so a future session
can pick it up without re-reading the original audit conversation.

In a future session (you or a fresh agent):

1. `cd` to the repo, check out the TASK-007 commit (`80d8e9a`) or main HEAD.
2. Open this file and look at the **Status** column for each item.
3. Pick a fix from the **Suggested fix order** section at the bottom.
4. For each item:
   - Read the **Issue** and **Recommendation**.
   - Apply the fix.
   - Mark the checkbox `[x]` and update **Status** to `Fixed` or `Deferred`.
   - Write the chosen approach in **Fix notes**.
5. Commit each fix (or batch) per the task workflow (`GIT.md`).
6. Repeat until all `[ ]` become `[x]` or `Deferred`.

The **Status** field is intentionally free-form so the next session can add context
(fix-up branch, blocked by task X, etc.). The audit is a snapshot of the state at
commit `80d8e9a`; do not edit historical items, only update **Status** and **Fix notes**.

---

## Critical (3)

### C1. SPEC compliance gap: warnings are dropped on success

- **Files:** `src/profile/importer.ts:215–240`, `src/persistence/schema.ts` (`profile_sources`)
- **Issue:** SPEC §13.4 says "Text-extraction **warnings or errors**" must be persisted. The schema
  has a single `textExtractionMessage` column. The `MarkdownExtractor` produces
  `warnings: string[]` (e.g., `markdown_contains_external_image_references`); the
  `ProfileImportService` returns them in `ImportedSource.warnings`, but they are
  **never written to the database**. A markdown CV with external image references produces
  a warning that is silently dropped after the import.
- **Recommendation:** Either (a) add a `warnings` JSON column to `profile_sources` and write the
  array; (b) add `textExtractionMessage`-style individual rows; or (c) document as a
  deliberate MVP deferral. Option (a) is the smallest change and keeps the existing
  `textExtractionMessage` column for backwards compatibility.
- **Status:** [x] Fixed
- **Fix notes:** Added a `warnings` JSON column to `profile_sources` (migration `0002_noisy_madame_web.sql`) with `NOT NULL DEFAULT '[]'`. `ProfileSourceRepository` parses/serializes the column with Zod validation. `updateExtraction` accepts an optional `warnings` field. `ProfileImportService` writes the array returned by the extractor on success (markdown `markdown_contains_external_image_references` now round-trips). The existing `textExtractionMessage` column is retained for backwards compatibility. Implemented in commit `354193d` (sub-task A).

### C2. Path-traversal edge case: basename `..` or `.`

- **Files:** `src/profile/file-copy.ts:9–27, 29–40`
- **Issue:** `path.basename()` correctly strips path separators on both POSIX and Windows, so
  `../../etc/passwd` → `passwd` (safe). But a user who controls the path on the command line
  could pass a path whose basename is literally `..` or `.`. The resulting
  `resolveSourceStoragePath` then returns `…/profile-sources/1/..` which `path.join` resolves
  to the parent directory `…/profile-sources/`. The copy would land in the wrong place and
  the `.tmp` rename would either fail or write to an unexpected location.
  - Verified: `path.basename('..')` returns `'..'`; `path.join('/data/profile-sources', '1', '..')` returns `/data/profile-sources`.
- **Recommendation:** Reject filenames whose `path.basename()` is `..` or `.`, or empty,
  with a clear `unsupported_format` error. Or, as defense-in-depth, validate the resolved
  destination is still inside `paths.profileSources.directory/{sourceId}/` after the join.
- **Status:** [x] Fixed
- **Fix notes:** `defaultFilenameFor` now throws `UnsupportedSourceFormatError` for empty paths, `.`, `..`, and basenames containing a NUL byte (no fallback to `cv.{ext}`). `resolveSourceStoragePath` adds a defense-in-depth check that the joined path is still inside `paths.profileSources.directory/{sourceId}/`. New tests cover each rejection case plus the basename-preservation paths. Implemented in commit `354193d` (sub-task A).

### C3. `EncryptedPDFException` in the detector is dead code

- **Files:** `src/profile/extractors/pdf.ts:14–17`
- **Issue:** I added `EncryptedPDFException` to `ENCRYPTED_PDF_NAMES` but pdf-parse v2 only
  exports `PasswordException` (verified in
  `node_modules/pdf-parse/dist/pdf-parse/esm/Exception.js:41`). The `EncryptedPDFException`
  branch will never fire. The string-based fallback (`message.includes('encrypted')`) still
  works, but the explicit-name check is dead.
- **Recommendation:** Remove `EncryptedPDFException` from the set. Either keep
  `PasswordException` only, or keep the string fallback only. Both is redundant.
- **Status:** [x] Fixed
- **Fix notes:** Removed `'EncryptedPDFException'` from `ENCRYPTED_PDF_NAMES` in `src/profile/extractors/pdf.ts`. The string-based fallback (`message.includes('encrypted')`) still covers the real signal. The existing `pdf-encrypted.test.ts` ensures the encrypted path is still detected via the remaining string fallback. Implemented in commit `354193d` (sub-task A).

---

## Important (9)

### I1. Redundant SHA-256 dedup lookup

- **Files:** `src/profile/importer.ts:142` + `src/persistence/repositories/profile-sources.ts:55`
- **Issue:** `ProfileImportService.importOne` calls `findBySha256` and short-circuits on hit.
  `ProfileSourceRepository.insert` **also** calls `findBySha256` and short-circuits on hit
  (lines 54–56). When the service calls `insert` after `findBySha256` returned null, the
  repository re-runs the same query. When the service's `findBySha256` returned non-null,
  the service never calls `insert` at all — so the repository's check is dead code in that
  path. The repository's check was added for "defense in depth" but is now redundant.
- **Recommendation:** Pick one location. Cleanest: remove the check from
  `ProfileSourceRepository.insert` (let the service own dedup) and document that `insert`
  is "INSERT OR ERROR" semantics. Alternatively, remove the service's pre-check and let
  the repository return the existing id. The second option is more robust against future
  callers.
- **Status:** [x] Fixed
- **Fix notes:** `ProfileSourceRepository.insert` is now strict INSERT-OR-ERROR. The `findBySha256` dedup lookup was removed; UNIQUE-constraint collisions now throw `DuplicateSha256Error` (extends `ApplicationError`, exit code 1). `ProfileImportService` already calls `findBySha256` before `insert`, so the API-level dedup behavior is preserved. The "is idempotent on sha256 conflict" repository test was replaced with "throws DuplicateSha256Error on collision". Implemented in commit `f901c69` (sub-task B).

### I2. `reused` count semantics overlap with `extracted`/`failed`

- **Files:** `src/profile/importer.ts:115–120`, surfaced in `src/cli.ts:146–148`
- **Issue:** A reused source counts toward `reused` **and** `extracted` (or `failed`). So
  `extracted: 1, reused: 1` for a 1-source batch where the source was reused. The numbers
  overlap. A user can subtract, but it's not obvious. The JSON output mixes a "did the
  extraction succeed" count with a "was this a re-import" count without explanation.
- **Recommendation:** Either (a) document the semantics in JSDoc on `ProfileImportCounts`;
  (b) split into `newExtracted` + `reusedExtracted` and `newFailed` + `reusedFailed`;
  (c) keep `reused` as a separate "no new work" tag and make `extracted`/`failed` count only
  the **new** sources. Option (c) is cleanest. Option (a) is the smallest change.
- **Status:** [x] Fixed
- **Fix notes:** Added JSDoc to `ProfileImportCounts` explaining that `reused` overlaps with `extracted`/`failed` (a reused source is in one of those buckets by its stored status). Includes a worked example. No behavioral change. Implemented in commit `8dde78a` (sub-task D).

### I3. Test pattern: process.exit mock + exitOverride interaction is fragile

- **Files:** `tests/cli/profile-import.test.ts:30–34, 56–66, 73–90`
- **Issue:** The test patches `process.exit` globally. Commander's `exitOverride()` throws a
  `CommanderError` instead of calling `process.exit`. The test catches both (`__exit__:` from
  the mock, and `CommanderError` from `exitOverride`). If a future test fails synchronously
  between `beforeEach` (patch) and `afterEach` (restore), the patches leak into other tests.
  The catch block has correct logic but the structure is fragile.
- **Recommendation:** Wrap each test in a `try/finally` that restores the patches, or extract a
  helper `withMockedProcessExit(async () => { … })`. Lower priority — works today, just brittle.
- **Status:** [x] Fixed
- **Fix notes:** Captured `process.exit`/`stdout.write`/`stderr.write` originals via a `captureOriginals()` helper at module load. The `afterEach` now only restores when the originals are defined, so a thrown `beforeEach` (e.g. `mkdtemp` failure) cannot leave the patches installed or restore them to `undefined`. The empty `try/finally` in `run()` is also gone (the cleanup is centralised). Implemented in commit `ed76ec5` (sub-task F).

### I4. `resolveRepoRoot` is unused

- **Files:** `src/persistence/resolve-migrations.ts:13–18`
- **Issue:** `resolveRepoRoot` is exported but only `resolveRepoRootForMigrations` is called
  externally. `resolveRepoRootForMigrations` is the only public API needed.
- **Recommendation:** Make `resolveRepoRoot` module-private (drop the `export`).
- **Status:** [x] Fixed
- **Fix notes:** Dropped the `export` keyword from `resolveRepoRoot` in `src/persistence/resolve-migrations.ts`. Only `resolveRepoRootForMigrations` is the public API. Implemented in commit `f869724` (sub-task E).

### I5. `void repositories;` is a no-op

- **Files:** `src/cli.ts:199`
- **Issue:** `repositories` is assigned in the try and used by `service.importSources`. The
  `void` in the finally is leftover from an earlier draft and serves no purpose — there's
  no unused-variable warning to silence (the variable is used in the try).
- **Recommendation:** Delete the line.
- **Status:** [x] Fixed
- **Fix notes:** Deleted the no-op `void repositories;` in `src/cli.ts`. Removing the line triggered `eslint(no-useless-assignment)` on the `let repositories` binding, so the `let` was converted to a `const` local inside the try block (the original `let` was only needed because the `finally` referenced the variable). The `Repositories` type import is now unused and is dropped. Implemented in commit `f869724` (sub-task E) for the void removal and `9a94542` (sub-task G) for the const refactor.

### I6. Default `BinaryFileSystem` uses dynamic import

- **Files:** `src/profile/file-system.ts:39–42`
- **Issue:** `removeFile` does `await import('node:fs/promises')` while every other method uses
  a top-level `import`. Inconsistent style and one extra microtask per call.
- **Recommendation:** Add `rm` to the top-level import and use it directly.
- **Status:** [x] Fixed
- **Fix notes:** Added `rm` to the top-level `import { ... } from 'node:fs/promises'` in `src/profile/file-system.ts`. The `removeFile` method now uses the static import directly. Implemented in commit `f869724` (sub-task E).

### I7. PDF bytes are read twice

- **Files:** `src/profile/importer.ts:139, 211`
- **Issue:** First, `createReadStream(absolutePath)` + `hashFileContents` streams the file for
  hashing. Then, after the copy, `fileSystem.readBytes(storedPath)` reads the entire file again
  for extraction. For a 10 MB CV this is one extra 10 MB buffer. The `ProfileImportService`
  could hash-while-copying via a stream pipeline to the destination, or extract from the
  buffer that was already read.
- **Recommendation:** Document as a known limitation or implement a streaming hash+copy+extract
  pipeline. Acceptable for MVP CVs (<5 MB).
- **Status:** [ ] Deferred
- **Fix notes:** Documented as a known limitation in the audit's "Deferred / out of scope" section. MVP CVs are typically < 5 MB. A streaming hash+copy+extract pipeline would eliminate the second read but is out of scope for this audit. Reassess if MVP CV size grows or if memory pressure becomes a real concern.

### I8. `text-pdf.pdf` content leaks into a real-world document

- **Files:** `tests/profile/fixtures/text-pdf.pdf`
- **Issue:** The PDF contains the literal text "Hello JobHunter". If the test PDF is ever
  displayed or printed in a debug context, the user sees placeholder text. More importantly,
  the extracted text `"Hello JobHunter\n\n"` is so short it doesn't exercise any interesting
  normalization (multi-line, blank-line collapse, etc.). A more realistic fixture would catch
  more real-world bugs.
- **Recommendation:** Acceptable for MVP. The test fixtures are documented as minimal.
- **Status:** [ ] Deferred
- **Fix notes:** Documented as a known limitation in the audit's "Deferred / out of scope" section. The current tests cover the success, image-only, malformed, and empty PDF cases; the extracted text `"Hello JobHunter\n\n"` is sufficient for the test assertions. Adding a richer fixture would be a separate `chore/richer-pdf-fixture` task.

### I9. `Image-only` PDF detection is heuristic

- **Files:** `src/profile/extractors/pdf.ts:6, 62, 92–97`
- **Issue:** The detection depends on pdf-parse returning whitespace-only text for image-only PDFs
  AND on the caller passing `pageJoiner: ''` to suppress the page-boundary marker
  (`-- 1 of 1 --`). If pdf-parse behavior changes or `pageJoiner: ''` is forgotten, the
  heuristic fails silently. There's no positive test for "PDF with mixed text+image but
  predominantly image".
- **Recommendation:** Add a comment in the file explaining the dependency, and add a unit test
  that asserts the `pageJoiner: ''` is passed.
- **Status:** [x] Fixed
- **Fix notes:** Added a comment above `getText({ pageJoiner: '' })` in `src/profile/extractors/pdf.ts` explaining that the empty joiner is required for the `EMPTY_TEXT_FALLBACK_PATTERN` to detect image-only PDFs. The comment also points to the matching assertion in `tests/profile/extractors/pdf.test.ts`. Added a new mock test that asserts `pdf-parse.getText` is called with `{ pageJoiner: '' }`. Implemented in commit `ed76ec5` (sub-task F).

---

## Minor (10)

### M1. `configShowCommand` has duplicate `JSON.stringify` in both branches

- **Files:** `src/cli.ts:104–112`
- **Issue:** Both `if (json)` and `else` write the exact same content. (Pre-existing, not introduced
  by TASK-007.)
- **Recommendation:** Collapse to one branch.
- **Status:** [x] Fixed
- **Fix notes:** Collapsed `configShowCommand` to a single branch. The `json` parameter is kept in the signature to match the documented `--json` flag behaviour; a `void json;` line documents that intent. Implemented in commit `9a94542` (sub-task G).

### M2. `summaryLine`/`cliJson` are misnamed

- **Files:** `src/cli.ts:133, 153`
- **Issue:** `summaryLine` returns a multi-line string, not a single line. `cliJson` returns an
  object, not JSON.
- **Recommendation:** Rename to `formatSummary` and `formatSummaryJson`.
- **Status:** [x] Fixed
- **Fix notes:** Renamed `summaryLine` → `formatSummary` and `cliJson` → `formatSummaryJson`. The new names describe what the functions return (a summary line and a JSON object), not how they are named. The rename also surfaces the parallel structure between the two formatters. Implemented in commit `9a94542` (sub-task G).

### M3. `pdf-encoder.ts` has unused `trailerStart` + `console.log` export

- **Files:** `tests/profile/fixtures/pdf-encoder.ts:46, 51–52, 66–68`
- **Issue:** `trailerStart` is assigned but unused; the `void trailerStart;` is a lint-silencing
  hack. `logTable` is a thin `console.log` wrapper that's only used by `build-fixtures.ts`.
- **Recommendation:** Remove `trailerStart` entirely. Move `logTable` to `build-fixtures.ts` (or
  inline a `console.log`).
- **Status:** [x] Fixed
- **Fix notes:** Removed the unused `trailerStart` local variable in `tests/profile/fixtures/pdf-encoder.ts` and the `void trailerStart;` lint-silencing hack. Inlined the trivial `logTable` helper into `build-fixtures.ts` as a single `console.log` call; the encoder no longer needs to export it. The on-disk PDF fixtures are unchanged (no byte regeneration). Implemented in commit `f869724` (sub-task E).

### M4. `noopLogger` is private but could be useful externally

- **Files:** `src/profile/importer.ts:64–68`
- **Issue:** Future tasks (e.g., TASK-008) may want to disable logging. They'd have to redefine
  their own noop.
- **Recommendation:** Export `noopLogger` from `src/profile/index.ts`.
- **Status:** [x] Fixed
- **Fix notes:** Exported `noopLogger` from `src/profile/index.ts` (and the underlying `importer.ts`). Future tasks that want to disable logging can now reuse it instead of redefining. Implemented in commit `ed76ec5` (sub-task F).

### M5. `defaultFilenameFor` edge cases

- **Files:** `src/profile/file-copy.ts:29–40`
- **Issue:** `path.basename('foo')` returns `'foo'` (correct). `path.basename('')` returns `''`
  (handled). `path.basename('..')` returns `'..'` (problematic — see C2). A filename with a NUL
  byte is passed through to the filesystem. The basename function also preserves spaces and
  unicode, which is fine.
- **Recommendation:** Reject `.` and `..` in the basename; strip NUL bytes. (The `..` and `.`
  rejection is also covered by C2.)
- **Status:** [x] Fixed
- **Fix notes:** Resolved together with C2 (sub-task A). `defaultFilenameFor` now rejects `.`, `..`, NUL bytes, and empty basenames; the `..` and `.` rejection is the core of M5. New tests cover each rejection case. Implemented in commit `354193d` (sub-task A).

### M6. `text-normalize.ts` collapses 3+ blank lines to 2

- **Files:** `src/profile/text-normalize.ts:18–20`
- **Issue:** The plan documented this rule, but it's not in SPEC. A markdown file with 3 blank
  lines between sections would be normalized to 2, which can surprise users.
- **Recommendation:** Document the rule in JSDoc. Acceptable as designed.
- **Status:** [x] Fixed
- **Fix notes:** Replaced the inline comments in `src/profile/text-normalize.ts` with a JSDoc block on `normalizeExtractedText` describing the normalization rules in order. The 3+ blank lines collapse to 2 rule is called out as "intentional but not documented in SPEC.md". Implemented in commit `ed76ec5` (sub-task F).

### M7. Fixture generator is committed but not exercised by tests

- **Files:** `tests/profile/fixtures/build-fixtures.ts`
- **Issue:** The script is committed but never run by the test suite. If someone modifies the
  encoder and re-runs it manually, the fixtures may silently change. A "regenerate and assert
  unchanged" test would catch this.
- **Recommendation:** Add a regeneration test that compares hash of the generated PDF against a
  pinned hash. Optional.
- **Status:** [ ] Deferred
- **Fix notes:** Documented as deferred in the audit's "Deferred / out of scope" section. A "regenerate and assert unchanged" test would catch silent fixture drifts but is optional. Can be added later as a `chore/fixture-regen-test` task.

### M8. `ImportedSource` `path` may diverge from `originalAbsolutePath` on Windows

- **Files:** `src/profile/importer.ts:127, 150, 168`
- **Issue:** `path.resolve(rawPath)` returns the OS-specific absolute path. On Windows, this is
  `C:\\Users\\…`. The DB column is just `text` — no normalization. The CLI summary prints the
  raw path.
- **Recommendation:** Acceptable. Document the platform-specific format.
- **Status:** [x] Fixed
- **Fix notes:** Added a JSDoc to `ImportedSource.path` explaining that the value is `path.resolve(rawPath)` and is OS-native (forward slashes on POSIX, backslashes on Windows). The CLI summary uses `.split(/[\\/]/)` to extract the basename for that reason. Implemented in commit `ed76ec5` (sub-task F).

### M9. Test name "imports a single Markdown file" vs the file extension

- **Files:** `tests/profile/importer.test.ts:52–71`
- **Issue:** Test name says "Markdown file" but the file has `.md` extension. Consistent.
- **Recommendation:** N/A — not a real issue, listed for completeness.
- **Status:** [x] N/A
- **Fix notes:** The audit notes the test name and the file extension are consistent (both "Markdown"). No change needed.

### M10. `ImportedSource.warnings` is on the result but unused

- **Files:** `src/profile/importer.ts:31`, surfaced at the CLI but never displayed
- **Issue:** The `ImportedSource.warnings` field exists and is set correctly, but the CLI summary
  (`summaryLine`) and the JSON output (`cliJson`) never display it. The user cannot see the
  markdown external-image warning. Related to C1 (warnings not persisted) — even the
  in-memory warning is invisible.
- **Recommendation:** Either remove the field, or surface it in the CLI summary and JSON. If
  kept, mention in the summary:
  `source_N  success  filename (1 warning: markdown_contains_external_image_references)`.
- **Status:** [x] Fixed
- **Fix notes:** The CLI summary line now appends `(N warning: code)` or `(N warnings: code1, code2)` to each source line. The annotation combines the existing `textExtractionMessage` (parenthesized) with the new warning suffix when warnings are present. The JSON output already included the `warnings` array per source (no shape change). Two new CLI tests cover the summary line and the JSON array, both for a markdown file with an external image reference. Implemented in commit `46be3f6` (sub-task C).

---

## Verification (5)

### V1. `pnpm format:check` fails on 37 files on main

- **Issue:** Pre-existing failure inherited from main. TASK-007 introduced zero new formatting
  violations (verified per-file on the changed scope).
- **Recommendation:** Open a `chore/format-repo` task to run `pnpm format` once.
- **Status:** [x] Out of scope
- **Fix notes:** Tracked as a separate `chore/format-repo` task per AGENTS.md §13. The files touched by this audit-fix branch format clean (the existing pre-existing 37-file failure is acknowledged but not addressed in this audit). Reopen as a standalone task when convenient.

### V2. `extractors/pdf.ts` calls `parser.destroy()` in finally — does that satisfy SPEC §40 "Close browser resources"?

- **Issue:** pdf-parse uses a Web Worker internally. `parser.destroy()` releases the worker. This
  is the right thing. ✓ Confirmed.
- **Status:** [x] Confirmed during this audit
- **Fix notes:** `pdf-parse` uses a Web Worker internally; `parser.destroy()` releases the worker. This satisfies SPEC §40 "Close browser resources". No change needed.

### V3. The `pdf-encoder.ts` uses `latin1` for PDF stream bytes

- **Files:** `tests/profile/fixtures/pdf-encoder.ts:28, 35, 39`
- **Issue:** PDF stream bodies are binary. `latin1` is byte-preserving (1 byte = 1 char), so this
  is correct. The current `pageContent` is ASCII-only, so no corruption. If a future fixture
  contains UTF-8 in a stream body, the encoder would silently corrupt it.
- **Recommendation:** Document the assumption in a JSDoc.
- **Status:** [x] Fixed
- **Fix notes:** Added a JSDoc to `tests/profile/fixtures/pdf-encoder.ts` documenting the `latin1` byte-preserving assumption. The current `pageContent` values are ASCII-only so the assumption holds; non-ASCII stream bodies would be silently corrupted by the encoder. Implemented in commit `ed76ec5` (sub-task F).

### V4. CLI test patches `process.stdout` and `process.stderr` without protecting concurrent tests

- **Issue:** Vitest runs tests in the same file sequentially by default, so this is fine. If the
  test file is ever run in parallel with other CLI tests, state could leak.
- **Recommendation:** Vitest is sequential by default; no action.
- **Status:** [x] Not an issue
- **Fix notes:** Vitest runs tests in the same file sequentially by default. No state leak risk.

### V5. The plan file and the task brief are duplicated

- **Issue:** ~30% content overlap. The plan is the work blueprint; the task brief is the
  implementation report. Both are needed for different audiences.
- **Recommendation:** Acceptable.
- **Status:** [x] Not an issue
- **Fix notes:** The plan (`TASK-007-...md`) and the task brief (the "Implementation results" section) are intentionally separate documents. Both are needed for different audiences.

---

## Positive findings (preserved for context — no checkboxes)

- **Atomic copy pattern** (`write to .tmp, rename, clean up on failure`) is the right shape.
- **SHA-256 dedup** is correctly content-based and the message says it's the documented behavior
  (SPEC §13.3, §27.2).
- **Error hierarchy** is consistent with the existing `ApplicationError` pattern. Exit codes are
  correctly mapped.
- **TDD coverage** is comprehensive: 40 new tests, 12 test files, every sub-task has tests. The
  encryption + malformed detection is tested via mocked `pdf-parse` (the only practical way
  without a real encrypted fixture).
- **`exitOverride` + `configureOutput`** is a clean way to make Commander errors map to SPEC §37
  exit code 2.
- **No `any` types** anywhere in the new code.
- **No new top-level dependencies** beyond the approved `pdf-parse@2.4.5`.

---

## Suggested fix order

This is a default priority for the next session. Skip items you've deferred.

1. **C1** — SPEC compliance, affects schema for TASK-008. Small change.
2. **C2** — path-traversal, small fix.
3. **C3** — dead branch cleanup, trivial.
4. **I4** — `void repositories;` no-op removal, trivial.
5. **I5** — `void repositories;` no-op removal, trivial.
   *(I4 and I5 are both trivial; do them together.)*
6. **I6** — `BinaryFileSystem.removeFile` dynamic import, trivial.
7. **I2** — `reused` count semantics clarification, small JSDoc or count refactor.
8. **I1** — redundant dedup, choose one location, small refactor.
9. **I3** — test pattern hardening, low priority but worth doing.
10. **I9** — heuristic dependency comment + test, small.
11. **I7, I8** — performance / fixture richness, document as known limitations or defer.
12. **M1–M10** — minor cleanups, batch into a single `chore(profile): clean up minor audit findings` commit.
13. **V1** — `pnpm format` on main, separate task.

## Deferred / out of scope

- **Profile-source cleanup** (orphaned rows from copy failures): per SPEC §3, "Profile-source
  cleanup is outside the MVP." Defer to a future `chore/profile-source-cleanup` task.
- **Re-import retry** for previously-failed files: per SPEC §13.3, the same SHA-256 always reuses
  the existing row. A `--force` flag or `jobhunter profile reset <source-id>` would be a separate
  feature. Defer to a future `feat/profile-reset` task.
- **OCR support**: per SPEC §3, OCR is outside the MVP. No action.
- **Streaming copy**: per the audit's I7, MVP CVs are small enough. Defer.
- **Larger PDF fixture**: per the audit's I8, a real CV would catch more bugs but the test
  matrix is sufficient. Defer.
