// Top-level entry for @jobhunter/core.
// Consumers should prefer the submodule exports declared in src/package.json.
// This stub exists so the workspace package's "." export resolves to a real module
// until a curated barrel is introduced.
export {};
